version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  runtime-version: 3.11.9
  command: gunicorn -b 0.0.0.0:8080 --timeout 300 --workers 1 --preload main:app
  network:
    port: 8000
