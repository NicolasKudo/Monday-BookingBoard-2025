version: 1.0
runtime: python3
build:
  commands:
    - pip install -r requirements.txt
    - pip install gunicorn
run:
  command: gunicorn -b 0.0.0.0:8080 --timeout 300 --workers 1 --preload --max-requests 1 main:app
  network:
    port: 8080
